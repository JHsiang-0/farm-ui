# Farm 前端推进任务

状态说明：`[x]` 已完成，`[ ]` 待完成。

## 基线与 P0

- [x] P0.1 环境变量、请求封装、Bearer Token 和统一错误处理。
- [x] P0.2 分页、Long ID、时间、文件大小和进度适配。
- [x] P0.3 API 模块统一、预签名下载和规划接口隔离。

## P1.1 登录与权限

- [x] 为业务路由增加 `requiresAuth` 和角色元数据。
- [x] 实现全局角色守卫，阻止直接访问无权限地址。
- [x] 按角色过滤布局菜单和 ADMIN 专属操作。
- [x] 完善登录失败、账号禁用、服务不可用和登录数据缺失提示。
- [x] 对“记住我”行为作出明确处理，避免未勾选时长期保存登录态。
- [x] 更新 Mock 登录场景和开发文档，不写入真实凭据。

## P1.2 打印机看板与控制

- [ ] 完成打印机分页和统一状态展示。
- [ ] 完成 ADMIN 配置、扫描、批量添加和网格位置维护。
- [ ] 按状态限制暂停、急停、取消和安全启动。
- [ ] 预留详情、历史和统计入口，不调用规划接口。

## P1.3 文件库

- [ ] 完善上传进度、限制、失败重试和当前目录刷新。
- [ ] 完成筛选、文件夹、删除、预签名下载和错误处理。
- [ ] 预留目录树、关联任务和安全预览入口。

## P1.4 任务流程

- [ ] 完成创建、派发、安全确认和安全启动。
- [ ] 完成取消、暂停和急停的状态限制。
- [ ] 统一任务状态、优先级和错误展示。
- [ ] 预留重试、重新排队和优先级调整入口。

## P1.5 WebSocket 与 Mock

- [ ] 接入 `/ws/farm-status` 和 Token 连接策略。
- [ ] 适配快照、设备状态、离线和任务状态事件。
- [ ] 完成断线重连、页面卸载和退出登录清理。
- [ ] 增加可重复的权限、资源、设备和存储错误场景。

## P2 稳定性与生产加固

- [ ] 增加请求层、路由权限、状态按钮和 WebSocket 测试。
- [ ] 完成端到端验收流程。
- [ ] 完成生产环境变量、CORS、预签名 URL 和 WebSocket 鉴权检查。

## 推进规则

每次只实现一个可验收的任务组：编码 → Mock/接口验证 → lint → build → 更新本文件和 `FRONTEND_TODO.md` → 中文提交。遇到后端规划接口时保持占位，并记录阻塞原因，不通过猜测接口继续开发。

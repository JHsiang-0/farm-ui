# T208-10 脱敏验收记录

验收日期：2026-09-05

## 环境与范围

- 浏览器：当前会话连接的 Chrome 浏览器自动化标签页。
- 地址：`http://127.0.0.1:5173`，Vite `mock` 模式。
- 后端契约依据：运行中的 `http://localhost:8080/v3/api-docs`（HTTP 200）与仓库 `API_HANDOFF.md`；本记录不包含账号、密码、Token、IP 白名单或真实业务标识。
- 数据范围：仅使用仓库内 Mock 的脱敏种子数据；未上传文件、未删除记录、未执行设备控制或密码修改。

## 逐页浏览器检查

以下页面均在同一标签页通过应用导航打开，并核对可见标题、主要字段、按钮名称、空白/状态文案和 DOM 横向溢出。登录页保留原有双栏视觉结构。

| 页面 | 路径 | 检查结果 |
| --- | --- | --- |
| 登录 | `/login` | 通过：用户名、密码、记住我、登录和错误提示可访问；原有视觉结构保留 |
| 工作台 | `/dashboard` | 通过：REST/WS 状态、统计卡片、趋势、活动任务和异常设备可见 |
| 全屏看板 | `/dashboard/fullscreen` | 通过：状态分布、任务进度、趋势、网格和断线提示可见 |
| 打印机 | `/printers` | 通过：筛选、协议/状态/耗材字段、详情和危险操作按钮可见 |
| 文件 | `/files` | 通过：目录树、文件元数据、上传、预览、下载和删除结果可见 |
| 任务队列 | `/tasks/queue` | 通过：排队任务、活动任务、状态、进度和控制操作可见 |
| 打印历史 | `/tasks/history` | 通过：状态、设备、时间、进度、结束时间和失败原因可见 |
| 批量派发 | `/batch-dispatch` | 通过：五步流程、无副作用预览、确认、部分成功和恢复项可见 |
| 用户 | `/users` | 通过：用户字段、ADMIN/OPERATOR、启停状态和权限限制可见 |
| 审计 | `/audit-logs` | 通过：操作者、动作、目标、时间、结果和详情字段可见 |
| 个人中心 | `/profile` | 通过：资料表单、密码规则、脏表单和成功后的会话处理可见 |

实际浏览器自动化可直接控制并报告的视口为 `1920×855`，该尺寸已对上述页面完成 DOM/可访问性与视觉检查。当前连接的自动化接口不提供 viewport emulation，因此 `375/768/1440` 未被记录为截图实测；这三档按 `src/styles/responsive.css` 的 `max-width: 639/767` 与 `min-width: 768/1024/1280/1920` 分支完成断点静态核对，未将静态核对冒充真实浏览器截图结果。

## 自动化验收覆盖

- `tests/t208Acceptance.test.js`：标准 HTTP 错误矩阵、空资源分页、ADMIN/OPERATOR/未登录路由权限、任务状态机、异步状态、WS 断序恢复和危险/部分成功页面契约。
- 现有测试继续覆盖主题 Token、Mock 完整业务链路、批量幂等恢复、文件上传结果、WebSocket 断线重连以及表单/权限规则。

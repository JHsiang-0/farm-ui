# FabMatrix Client Frontend UI Specification

> 版本：1.0  
> 状态：后续前端开发与 UI 验收基线  
> 适用范围：FabMatrix Client 全部业务页面、Electron 壳内页面和公共交互组件  
> 技术基线：Vue 3、TDesign Vue Next、Pinia、Vue Router、Axios、现有 Electron 壳

## 1. 文档目标

本规范用于保证 FabMatrix Client 的业务界面持续采用 TDesign Vue Next 默认设计语言，并以 TDesign Starter Vue Next 基础仪表盘为布局参考。

本规范同时约束：

- 信息架构和用户工作流
- 页面布局、密度和响应式行为
- TDesign 组件选择与操作层级
- Loading、Empty、Error 和实时状态
- API 数据映射、权限和状态机展示
- 浏览器和 Electron 的视觉验收

登录页和首次管理员初始化页已有的双栏布局、动画角色、品牌视觉和表单风格属于保护范围。业务壳统一时不得将其改造成普通后台登录模板。

## 2. 不可违反的原则

### 2.1 数据与契约

1. 前端数据只能来自真实后端 Swagger/OpenAPI 和 `API_HANDOFF.md`。
2. 不得新增接口、字段、状态、统计值或业务结果来填补视觉空白。
3. 页面不得直接依赖后端 DTO 的展示形态；必须通过 API adapter 或 selector 形成页面 View Model。
4. API 失败、字段缺失和无数据必须被明确表达，不得使用零值、假名称、假温度或默认成功状态掩盖问题。
5. REST 是列表和操作结果的权威来源；WebSocket 只负责正式协议范围内的实时覆盖和事件同步。
6. 后端契约变更必须先更新交接文档和测试，再更新页面。

### 2.2 视觉与组件

1. 业务页面优先使用 TDesign Vue Next 官方组件和默认 token。
2. 项目自定义 CSS 只负责业务布局差异，不得重新创造颜色、阴影、圆角、按钮或状态体系。
3. Tailwind 可以在迁移期间保留，但不得继续承担业务页的主色、状态色和核心布局规则。
4. 同一种业务控件只能有一种主要视觉表达，例如状态统一使用 Status Tag，确认统一使用 Popconfirm/Dialog。
5. 组件数量不是完成度；每个容器都必须服务于信息分组、滚动边界或操作流程。

### 2.3 反馈与可恢复性

每个异步流程至少定义以下状态：

- 初始加载
- 加载成功且有数据
- 加载成功但无数据
- 请求失败
- 无权限
- 业务冲突或参数校验失败
- 刷新中
- 实时数据断开或陈旧

错误必须提供用户能执行的下一步，例如重试、返回、重新选择或联系管理员。

## 3. 应用信息架构

### 3.1 一级导航

当前业务域建议保持以下分组：

| 分组 | 页面 | 用户目标 |
| --- | --- | --- |
| 工作台 | 概览仪表盘 | 快速了解设备、任务和告警 |
| 打印机 | 打印机管理 | 查询、维护、查看和控制设备 |
| 文件 | 文件库 | 管理文件、预览、下载和创建任务 |
| 任务 | 任务队列、打印历史 | 派发、跟踪和追溯任务 |
| 批量派发 | 批量派发 | 预览并确认多文件、多设备计划 |
| 管理中心 | 用户管理、操作日志 | 管理账号和审计记录 |
| 个人中心 | 个人中心 | 维护当前用户资料和密码 |

导航项必须对应真实路由和真实功能。没有后端或产品契约支撑的入口不能伪装成已实现功能；如必须保留入口，应明确显示规划中状态。

### 3.2 权限边界

- `ADMIN`：可执行管理中心、打印机维护和后端允许的管理操作。
- `OPERATOR`：可执行本人权限范围内的文件、任务和设备作业操作。
- 菜单过滤、按钮显隐和 API 鉴权必须同时存在。
- 页面不得只相信本地持久化角色；启动和恢复会话必须以 `/api/v1/auth/me` 为准。
- 403 页面应解释“无权限”，不能静默跳转或显示空白。

## 4. App Layout 规范

### 4.1 桌面布局

使用一个明确的 TDesign Layout 层级：

```text
App Layout
├── Sidebar / Aside
├── Main Layout
│   ├── Header
│   └── Content
│       ├── Breadcrumb（需要层级时）
│       └── Route View
```

默认尺寸：

| 区域 | 桌面 | 窄屏 |
| --- | ---: | ---: |
| Sidebar | 232px | 覆盖式 Drawer，宽度不参与主布局 |
| 折叠 Sidebar | 64px | 不适用 |
| Header | 64px | 56px 以上 |
| Content 内边距 | 24px | 16px |
| 内容最大宽度 | 1600px | 100% |

### 4.2 滚动所有权

滚动必须遵循“一个区域一个滚动所有者”：

1. App Shell 负责视口高度，不负责业务表格内容滚动。
2. 页面内容默认自然增长；只有明确的工作区才设置边界高度。
3. 表格使用一个确定的 `maxHeight` 或固定高度容器。
4. Drawer 的 Body 独立滚动，Header 和 Footer 固定。
5. 目录树和文件列表可以分别滚动，但必须有清晰的高度边界。
6. 禁止同时叠加 `height: 100%`、`overflow: hidden`、绝对定位表格和多层 Loading 父容器。
7. 页面底部的分页、操作栏和最后一行数据必须完整可见。

### 4.3 窄屏行为

- Sidebar 变成覆盖式导航 Drawer，关闭时不占用内容宽度。
- Header 的非核心图标进入菜单或隐藏，但不得遮挡用户菜单和连接状态。
- 页面标题、说明和主按钮允许换行。
- Toolbar 自动换行；查询和重置保持可发现。
- 表格允许水平滚动，但第一列、状态列和操作列要保持可达。
- Drawer 宽度不得超过视口；表单字段单列排列。
- 不允许页面级水平溢出。

## 5. Sidebar / Header

### 5.1 Sidebar

- 使用 TDesign Menu/Aside 的 active 状态表达当前路由。
- 分组标题使用低强调文本，菜单项使用统一图标和行高。
- active 项同时体现背景、文字和图标状态，不只依赖颜色。
- 菜单顺序应符合用户工作流：看板 → 设备 → 文件 → 任务 → 管理。
- 移动端关闭后不保留桌面 Aside 的固定宽度。

### 5.2 Header

Header 只承载全局能力：

- Sidebar 折叠
- 当前服务器/实时连接状态
- 通知或系统提示
- 帮助入口（必须有真实功能）
- 用户菜单、个人中心、退出登录

所有图标按钮必须有可访问名称和 Tooltip。没有实现的帮助、设置或通知不能表现为可用入口。

## 6. Page Header 与页面骨架

### 6.1 标准 Page Header

每个页面只允许一套页面标题：

```text
页面标题
简短说明                                      主操作  次操作
```

要求：

- 标题使用页面业务名称，不重复渲染。
- 说明最多两行，解释当前页面的任务目标。
- 每个页面最多一个 Primary 主操作。
- 刷新、导入、创建等作为次级操作。
- 只有确实存在层级关系时使用 Breadcrumb。
- 不得在 Page Header、Card Header 和内容区重复写同一个标题。

### 6.2 页面 Card 使用原则

Card 只用于以下情况：

- 明确的信息分组
- 独立的表单或配置区
- 需要强调的统计区
- 具有独立 Loading/Empty/Error 生命周期的区域

不得把整页所有内容都包在 Card 中，也不得在只有一两行数据时强制 Card 填满整个视口。数据页面优先采用 Page Header + Toolbar + Table。

## 7. Toolbar 与筛选

### 7.1 标准结构

```text
Toolbar
├── 筛选条件（左）
├── 关键字搜索
└── 查询 / 重置 / 刷新（右）
```

- 使用 TDesign Form 的 inline 模式。
- 标签、占位文本和字段顺序与 API 查询参数一致。
- 查询按钮是筛选区的主要动作，刷新是页面级次操作。
- 查询中只禁用当前查询动作，不阻塞整个页面。
- 查询失败保留用户输入和已有数据。
- 窄屏时折叠为可展开筛选区。
- 不为一个简单搜索栏再套一层厚重 Card。

### 7.2 页面专用约束

- 服务器连接：IP 和端口分开输入；“测试”和“保存并连接”是两个独立动作。
- 打印机管理：名称、状态等字段只发送 OpenAPI 定义的查询参数。
- 文件库：文件名字段使用 `fileName`，材质使用 `materialType`，目录使用 `parentId`。
- 打印历史：状态、打印机、关键词和时间范围必须与任务分页接口一致。

## 8. Table 规范

### 8.1 选型

- 优先使用 TDesign 原生 `t-table`。
- `TdTable` 兼容层只用于已有列插槽无法无风险迁移的场景，并限制新增引用。
- 表格 View Model 负责格式化名称、状态、时间、单位和操作能力。

### 8.2 列设计

列表只展示帮助用户做决策的字段：

- 核心身份：ID、名称或文件名
- 当前状态
- 关键业务属性
- 最近更新时间或进度
- 操作

低频字段进入详情 Drawer，不为了“完整映射接口”而横向堆满表格。

### 8.3 交互

- 操作列必须有明确的“详情”或对应动词按钮。
- 行点击可以作为辅助入口，不能是唯一详情入口。
- 危险操作使用 Popconfirm/Dialog，并说明影响范围。
- 服务端分页使用 API 返回的总数和页数，不在前端伪造分页。
- Loading、Empty、Error 和刷新状态在表格区域内表达。
- 横向滚动时核心信息和操作仍然可达。
- 最后一行和分页不得被固定操作列或遮罩覆盖。

## 9. Form 规范

- 使用 TDesign Form、Input、Select、DatePicker、Upload 等官方组件。
- 表单字段必须有可见标签和必要的帮助文本。
- 校验错误显示在字段附近，并与输入控件建立可访问关联。
- 提交按钮显示 Loading，防止重复提交。
- 保存失败不清空用户输入。
- 成功后以权威 API 结果刷新相关 Store 或列表。
- 不在 UI 中添加后端不存在的业务字段。
- 密码、API Key 等敏感字段不得回显；编辑时遵守后端“省略或空白表示保留”的契约。

## 10. Dialog 与 Drawer

### 10.1 Dialog

用于短表单、上传、创建和确认动作：

- 标题使用动作词，例如“新增打印机”“创建任务”。
- Footer 统一为取消 + 主动作。
- 主动作只执行一次，成功后关闭并刷新。
- 业务错误显示在 Dialog 内部，Toast 仅作为补充。
- 删除、取消任务等危险操作必须二次确认。

### 10.2 Drawer

用于打印机、任务、文件和用户详情：

- Header 固定，标题使用真实名称；名称缺失时使用稳定回退文案，例如“打印机详情”。
- Body 独立滚动，Footer 固定。
- 详情数据不得由列表行拼接伪造。
- 基本资料、实时状态、当前任务、历史和统计使用 Tabs/Descriptions/Statistic/Table 分组。
- 每个详情数据区独立处理加载、空数据和错误。
- 支持 ESC 关闭、关闭按钮和焦点返回。
- Drawer 打开后必须避免遮挡关键错误提示和全局操作。

## 11. Tabs 与工作流

Tabs 只用于同级内容：

- 待派发任务与活动任务
- 打印机基本信息、历史、统计
- 文件详情与关联任务

任务状态必须遵循后端定义：

```text
QUEUED
ASSIGNED
UPLOADING
READY
PRINTING
PAUSED
RECONCILING
COMPLETED
FAILED
CANCELLED
```

`QUEUED` 待派发列表不能混入活动任务；活动任务不能用一个含义模糊的“全部队列”接口代替。批量派发使用 TDesign Steps，但每一步只展示当前目标，避免把所有步骤内容堆在首屏。

## 12. Status Tag 规范

建立全局唯一映射：

```text
后端枚举 → 用户文案 → TDesign 主题 → 图标/辅助说明
```

打印机状态：

```text
OFFLINE、IDLE、PREPARING、PRINTING、PAUSED、ERROR、UNKNOWN
```

约束：

- `ONLINE` 不作为持久化打印机状态展示。
- 状态不能只靠颜色区分。
- `UNKNOWN`、陈旧数据和实时断开要有明确文本。
- 日志动作等原始枚举要转换为用户可理解文案。
- 状态颜色、文案和图标必须跨 Dashboard、Table、Drawer、通知保持一致。

## 13. Loading / Empty / Error / Stale

### 13.1 Loading

- 首次页面加载：Skeleton 或区域 Loading。
- 表格刷新：保留旧数据，显示局部 Loading。
- Dialog 提交：只 Loading 当前提交按钮和必要区域。
- Drawer：先显示标题和结构，再在 Body 内显示详情 Loading。

### 13.2 Empty

Empty 必须说明“为什么为空”和“下一步做什么”：

- 无打印机：添加打印机或扫描设备
- 无文件：上传文件或新建文件夹
- 无任务：创建任务或返回文件库
- 无历史：调整筛选条件

### 13.3 Error

至少区分：

- 401：会话失效，清理会话并回到登录
- 403：当前用户无权限
- 404：资源不存在或不可见
- 409：状态冲突，需要刷新后重试
- 422：输入或状态流转不合法
- 503：后端、存储或设备服务不可用

错误区域应提供重试或返回操作。不要把所有错误都转换为同一个 Toast，也不要在失败后渲染空白成功页面。

### 13.4 实时状态

WebSocket 断开、陈旧或恢复中时：

- 显示全局或页面级连接状态
- 保留最后一个权威 REST 数据
- 不用过期实时数据伪装实时在线
- 重连后重新合并快照和增量事件
- 未知任务事件不能凭事件构造缺字段任务，应标记列表需要刷新

## 14. 页面级规范

### 14.1 概览仪表盘

- 首屏展示设备状态、活动任务和关键告警。
- 统计卡片必须能追溯到真实接口数据。
- 图表缺少数据时显示 Empty/说明，不显示假趋势。
- 全屏模式支持 ESC、退出按钮和 Electron 缩放。

### 14.2 打印机管理

- Toolbar：刷新、未分配设备、扫描局域网设备、新增打印机。
- Table 展示核心设备字段，详情放入 Drawer。
- 详情调用 `GET /api/v1/printers/{id}`。
- 历史调用 `/history`，统计调用 `/statistics`。
- 扫描和批量添加必须展示逐项结果，不把部分失败显示为全部成功。

### 14.3 文件库

- 目录树使用真实 `FileNodeVO[]` 或契约支持的目录查询。
- 文件列表使用 `folder`，不得依赖 `isFolder`。
- 文件名查询使用 `fileName`，不得发送 `keyword`。
- 网格是可选视图，表格是大数据量的优先视图。
- 预览、缩略图和下载必须通过独立接口获取短期 URL。
- 批量上传逐项展示成功、失败原因和可重试性。

### 14.4 任务队列与历史

- 待派发页只显示 `QUEUED`。
- 活动任务显示 `ASSIGNED` 到 `RECONCILING` 的运行中状态。
- 历史页使用分页和完整筛选，结束时间读取 `completedAt`。
- 任务详情调用真实详情接口，不由列表行拼接。
- 操作入口必须根据合法状态动作矩阵显示。

### 14.5 批量派发

使用五步工作流：

1. 选择文件和打印机
2. 配置策略
3. 无副作用预览
4. 确认执行
5. 查看逐项结果

预览不得创建任务或占用打印机。确认必须使用服务端返回的 `planId`、版本和确认令牌；部分失败必须保留成功项并按 `retryable` 决定重试入口。

### 14.6 用户、日志和个人中心

- 用户管理只展示当前角色可管理的字段和动作。
- 操作日志将原始动作转换为用户可理解文案，并保留时间、操作者和目标。
- 个人资料和密码修改使用两个清晰的表单区域，不用大片空白撑满页面。
- 修改成功后同步更新当前用户 Store。

### 14.7 登录与首次初始化

- 保留现有双栏、动画角色、品牌和表单视觉。
- 登录页首先读取 `GET /api/v1/auth/setup/status`。
- 当 `setupAvailable=true` 时，在同一认证入口提供首次管理员初始化流程。
- 初始化调用 `POST /api/v1/auth/setup/admin`，成功后使用后端返回会话进入系统。
- 不新增公开注册流程。
- 初始化状态失败时提供原位重试，不用空白页或无上下文 Toast。

## 15. Tailwind 与自定义 CSS 管理

Tailwind 不立即删除，采用渐进迁移：

### 允许保留

- 少量布局辅助类
- 登录页既有视觉实现
- 不影响 TDesign token 的局部响应式工具

### 应迁移到 TDesign 或语义 Token

- 主色、成功色、警告色、危险色
- 按钮、Tag、Progress 的视觉
- Card、Table、Form 的间距和边框
- 页面标题、工具栏和内容区布局
- 业务页中重复的 flex、grid、padding、margin 组合

### 禁止新增

- 业务页面硬编码颜色
- 与 TDesign Button/Tag/Alert 重复的自定义组件
- 没有滚动边界说明的 `overflow: hidden`
- 通过绝对定位修复表格高度
- 为填充空白而增加无业务意义的 Card

## 16. 可访问性要求

- 所有表单控件有可见标签或可访问名称。
- 图标按钮必须有 `aria-label` 或 Tooltip。
- 键盘可以完成导航、筛选、提交、关闭 Dialog/Drawer 和确认操作。
- Focus 状态清晰可见，Drawer/Dialog 关闭后焦点返回触发元素。
- 状态不只依靠颜色传达。
- 错误文本与字段建立关联，动态错误可被辅助技术感知。
- 表格列标题、分页和操作按钮顺序符合阅读顺序。
- 动画提供减少动态效果的兼容策略；登录页保留视觉时仍应避免影响输入和键盘操作。

## 17. 自动化与视觉验收

### 17.1 必测视口

```text
375×812
768×1024
1024×768
1440×900
1920×855
```

### 17.2 测试要求

自动化测试必须：

- 使用真实用户点击和键盘操作，不用 `dispatchEvent` 替代核心交互。
- 使用真实 Mock/API 数据量，不复制 DOM 行制造滚动假象。
- 等待页面稳定、接口完成和 Loading 消失后再截图。
- 覆盖成功、空数据、错误、403、409、503、实时断开和恢复。
- 验证详情、编辑、删除、上传、下载和任务操作的真实入口。
- 同时验证 5173 Mock、实际前端运行地址和 Electron 壳的环境差异。

### 17.3 人工验收清单

- 页面是否只有一个明确滚动所有者？
- 最后一行、分页和底部操作是否可见？
- 首屏是否只有一个主操作？
- 空数据时是否告诉用户下一步？
- 错误时是否保留上下文并提供恢复动作？
- 详情入口是否显式存在？
- 是否出现 `undefined`、原始枚举、假零值或假成功？
- 窄屏是否仍能完成主流程？
- 视觉是否由 TDesign token 和组件主导？
- 登录和首次初始化的既有视觉是否保持？

## 18. 整改优先级

### P0：发布阻断项

1. 统一 App Shell、Page Shell、Table、Card 的滚动模型。
2. 确认开发地址、Mock 地址、实际后端和 Electron 使用同一版本前端资源。
3. 修复打印机详情显式入口、真实字段映射和标题回退。
4. 修复任务队列的状态语义、巨大空白和隐藏操作。
5. 确认文件库已经使用最新实现，不再加载旧 Bundle 或旧组件。
6. 补齐关键页面的 Loading、Empty、403、409、503 状态。

### P1：体验整改项

1. 文件库改为表格优先、网格可选。
2. 打印机表格减少低优先级列，详情使用分组 Drawer。
3. 任务队列和历史页面使用自然高度数据表格。
4. 批量派发改为聚焦当前步骤的向导。
5. 用户、日志和个人中心减少无意义留白。
6. 统一操作列、状态标签、筛选栏和分页。

### P2：体系治理项

1. 渐进式收缩业务页 Tailwind 和重复自定义 CSS。
2. 建立共享 PageHeader、Toolbar、StatusTag、AsyncState 组件。
3. 为 API 建立稳定的 View Model adapter 层。
4. 增加键盘、焦点、真实数据量和异常状态 E2E。
5. 统一 OpenAPI、Swagger UI 和 `API_HANDOFF.md` 的版本与访问说明。

## 19. 开发交付门禁

每个页面或子任务交付前必须完成：

1. 读取并核对 `API_HANDOFF.md` 和运行时 OpenAPI。
2. 说明信息架构、布局、交互、Loading、Empty、Error 和状态同步影响。
3. 只修改当前任务范围内文件。
4. 涉及 JavaScript/Vue 时执行 `npm run lint`。
5. 执行 `npm run build`。
6. 执行相关单元测试和 Playwright/Electron 测试。
7. 执行 `git status --short` 和 `git diff --check`。
8. 只暂存当前任务文件，不使用 `git add .`。
9. 创建一次 Conventional Commit 中文提交。
10. 不执行 `git push`。

## 20. 契约参考

- [API_HANDOFF.md](./API_HANDOFF.md)
- [.kiro/specs/farm-ui-v2/requirements.md](./.kiro/specs/farm-ui-v2/requirements.md)
- [.kiro/specs/farm-ui-v2/design.md](./.kiro/specs/farm-ui-v2/design.md)
- [.kiro/specs/farm-ui-v2/tasks.md](./.kiro/specs/farm-ui-v2/tasks.md)
- [TDesign Starter Vue Next](https://tdesign.tencent.com/starter/vue-next)
- [TDesign Starter 开发规范](https://tdesign.tencent.com/starter/docs/vue-next/develop-rules)
- [TDesign Vue Next 组件文档](https://tdesign.tencent.com/vue-next/overview)


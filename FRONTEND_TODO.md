# Farm 前端开发 TODO

版本：v1.0
依据：[`TODO.md`](./TODO.md)、[`API_HANDOFF.md`](./API_HANDOFF.md)
更新时间：2026-09-02

## 使用说明

- `[x]` 已完成或已有基础实现
- `[ ]` 待开发
- `P0` 契约、鉴权和联调阻塞项
- `P1` 第一版业务功能
- `P2` 稳定性、体验和生产加固

前端只使用 `ADMIN`、`OPERATOR` 两种角色。任务状态只使用：
`QUEUED`、`ASSIGNED`、`READY`、`PRINTING`、`PAUSED`、`COMPLETED`、`FAILED`、`CANCELLED`。

## 当前前端基线

- [x] Vue 3、Vite、Vue Router、Pinia、Axios、TDesign Vue Next 已配置。
- [x] 已有登录页、农场看板、打印机管理、文件库、任务队列和任务历史页面。
- [x] 已有 Axios 请求封装、Bearer Token 注入、401 登出和路由登录守卫。
- [x] 已有 mock 数据及 `VITE_USE_MOCK=true` 的请求分流，可用于页面开发。
- [x] 已有打印机 Store、网格 Store、实时状态 Store 和 WebSocket 重连基础能力。
- [ ] 清理 `src/api/file.js` 与 `src/api/printFile.js` 的重复职责，统一文件 API 模块。
- [ ] 更新 API 模块中的 JSDoc，使字段、状态和接口地址与 `API_HANDOFF.md` 一致。
- [ ] 补齐用户管理、个人资料和修改密码页面及 API。
- [ ] 将当前页面中的临时 TODO、假数据映射和直接 `console` 调试逻辑收敛到正式实现。

## P0：联调前契约对齐

### P0.1 环境与请求基础层

- [x] 使用 `.env.local` 配置本地联调地址，不提交环境文件或敏感信息：

  ```env
  VITE_API_TARGET=http://192.168.0.57:8080
  VITE_WS_TARGET=ws://192.168.0.57:8080
  VITE_WS_URL=ws://192.168.0.57:8080/ws/farm-status
  VITE_USE_MOCK=false
  ```

- [x] 保持业务 API 使用 `/api/v1` 路径，并通过 Vite 代理转发 `/api` 和 `/ws`。
- [x] 统一请求成功返回 `{ code, message, data, timestamp }` 的解析方式。
- [x] 统一处理 HTTP 及业务错误码：`400`、`401`、`403`、`404`、`409`、`422`、`500`、`503`、`10001`、`10002`、`5003`、`5004`。
- [x] 401 清除登录态并跳转登录页；403 显示无权限；设备离线、设备忙碌和对象存储错误显示可操作提示。
- [x] 对重复提交增加按钮 loading 和请求锁，避免重复创建任务、删除文件或急停。

### P0.2 数据格式适配

- [ ] 新增统一分页适配器，兼容后端迁移前的 `current/size` 和目标格式 `pageNum/pageSize`。
- [ ] 所有列表请求统一传 `pageNum/pageSize`，默认 `pageSize` 不超过 100。
- [ ] Long 类型 ID 在展示、路由参数和比较时按字符串安全处理。
- [ ] ISO-8601 时间统一按 Asia/Shanghai 展示；WebSocket 时间戳按 Unix 毫秒转换。
- [ ] 文件大小使用字节转换为可读单位，进度统一按 `0-100` 百分比处理。
- [ ] 前端不读取或展示响应中的 `apiKey`、`rustfsKey`、内部存储路径等字段。

### P0.3 API 模块统一

- [ ] 新前端代码统一使用 `POST /api/v1/print-jobs` 创建任务。
- [ ] 旧 `/api/v1/print-jobs/create` 只保留兼容调用，不再新增引用。
- [ ] 文件下载先调用下载接口获取预签名 URL，再处理跳转或 Blob 下载；处理 URL 过期和权限失败。
- [ ] 明确接口返回 `data=null` 时的处理，禁止把成功的空响应当成异常。
- [ ] 后端规划接口未实现前，页面不得直接调用：打印机详情、历史、统计、恢复、任务重试、重新排队、优先级和目录树。

## P1：第一阶段直接联调

### P1.1 登录与权限

- [ ] 登录表单接入 `POST /api/v1/auth/login`，展示登录失败、账号禁用和服务不可用提示。
- [ ] 登录成功保存 token、用户 ID、用户名和角色；退出时清除持久化登录态。
- [ ] 菜单和按钮按角色控制：
  - [ ] `ADMIN`：用户管理、打印机配置、扫描、删除、网格位置维护。
  - [ ] `OPERATOR`：查看打印机、文件操作、创建和控制打印任务。
- [ ] 页面路由增加角色校验，不能只依赖按钮隐藏保护权限。
- [ ] 密码表单提示与后端规则一致：6-20 位，必须包含大写字母、小写字母和数字。

### P1.2 打印机看板与管理

- [ ] 使用 `GET /api/v1/printers/page` 获取初始设备列表和分页数据。
- [ ] 看板展示名称、机器编号、IP、协议、统一状态、温度、当前任务和进度。
- [ ] 统一展示打印机状态：`OFFLINE`、`IDLE`、`PREPARING`、`PRINTING`、`PAUSED`、`ERROR`、`UNKNOWN`。
- [ ] ADMIN 接入新增、编辑、删除、局域网扫描、批量添加和网格位置保存。
- [ ] 扫描和批量添加展示每台设备的成功或失败原因。
- [ ] 控制按钮按状态禁用：暂停、恢复、取消和急停不得在不允许状态下发送请求。
- [ ] 急停、取消和删除操作增加二次确认，失败时保留页面上下文并提示明确原因。
- [ ] 预留打印机详情、状态历史和统计入口，等待后端规划接口完成后接入。

### P1.3 文件库

- [ ] 文件分页接入 `POST /api/v1/print-files/page`，支持名称搜索和 `materialType` 筛选。
- [ ] 上传 G-code/BGCODE，展示上传进度、文件大小限制、扩展名错误和对象存储失败。
- [ ] 上传时绑定当前目录 `parentId`，成功后刷新当前目录而不是整个页面盲目重载。
- [ ] 接入创建文件夹、进入目录、返回上级和当前目录内容查询。
- [ ] 单个删除、批量删除增加确认，并处理已关联任务、无权限和文件不存在等结果。
- [ ] 文件详情展示切片参数、耗材、喷嘴、温度、预估时间、打印次数和成功率。
- [ ] 下载使用后端预签名 URL；处理 URL 过期、文件不存在和跨域失败。
- [ ] 预留文件关联任务、目录树和安全预览入口，等待规划接口完成。

### P1.4 任务队列与打印流程

- [ ] 任务队列接入 `GET /api/v1/print-jobs/queue`，按优先级和创建时间展示。
- [ ] 任务历史接入 `POST /api/v1/print-jobs/page`，支持状态、打印机和时间筛选。
- [ ] 新建任务选择文件，可选打印机；不指定打印机时创建为 `QUEUED`。
- [ ] 页面只显示冻结后的 8 个任务状态，不显示 `PENDING`、`CANCELED` 或把 `PREPARING` 当作任务状态。
- [ ] 实现安全打印流程：
  - [ ] `QUEUED -> ASSIGNED`：选择空闲打印机并调用安全派发。
  - [ ] `ASSIGNED -> READY/PRINTING`：现场确认安全后上传或启动打印。
  - [ ] 支持取消、暂停和急停，并按状态限制操作。
- [ ] 任务详情展示文件、打印机、发起人、操作员、进度、时间和错误原因。
- [ ] 预留重试、重新排队和调整优先级按钮，后端接口完成后再启用。

## P1：实时状态接入

- [ ] WebSocket 使用正式地址 `/ws/farm-status`，不再新增 `/ws` 引用。
- [ ] 携带当前 token 建立连接；后端鉴权未完成前仅在 mock/dev 环境使用匿名兼容逻辑。
- [ ] 适配统一消息结构：`type`、`printerId`、`timestamp`、`data`。
- [ ] 处理 `SNAPSHOT`：连接成功后用全量快照初始化设备状态。
- [ ] 处理 `PRINTER_STATUS`：只更新对应打印机，不覆盖 REST 返回的静态配置。
- [ ] 处理 `PRINTER_OFFLINE`：将设备标记为离线并显示检查/重连提示。
- [ ] 处理 `JOB_STATUS`：局部更新任务状态、进度和错误原因，不重复请求整个列表。
- [ ] 页面卸载、退出登录和切换农场看板时销毁连接和定时器。
- [ ] 连接断开时显示“数据可能不是最新”，使用指数退避重连并避免重复连接。
- [ ] 后端 WebSocket 新协议完成前，mock 增加快照、状态变化、离线和任务状态事件样例。

## P1：模拟数据与开发模式

- [x] 提供 ADMIN、OPERATOR、打印机、文件和任务种子数据。
- [x] mock 覆盖登录、打印机分页/配置、文件分页/上传/文件夹/删除/下载、任务队列/分页/创建/派发/确认/启动/取消和设备暂停/急停。
- [ ] mock 与真实接口保持相同的响应包装、错误码和状态转换。
- [ ] 增加 401、403、404、409、422、10001、10002、5003、5004 的可重复演示场景。
- [ ] 增加 mock WebSocket 事件流，覆盖连接、快照、离线、设备状态和任务状态。
- [ ] 验证 mock 数据修改只存在于当前浏览器会话，提供重置 mock 状态入口或开发命令。
- [ ] 补充 mock 登录账号说明到开发文档，禁止把真实密码写入版本控制。

## P2：体验、测试与生产加固

### P2.1 交互体验

- [ ] 完善加载、空数据、无权限、网络错误、服务维护和离线状态页面。
- [ ] 文件上传支持取消、失败重试和重复文件名提示。
- [ ] 任务状态变化显示操作反馈，不使用静默失败。
- [ ] 打印机详情、文件详情和任务详情抽屉在刷新后保留当前上下文。
- [ ] 看板适配局域网大屏和窄屏，保证网格、表格和操作按钮可用。

### P2.2 前端验证

- [ ] 增加请求层和分页适配器测试。
- [ ] 增加路由登录守卫和 ADMIN/OPERATOR 权限测试。
- [ ] 增加任务状态按钮可用性测试，覆盖非法操作和重复点击。
- [ ] 增加 WebSocket 消息解析、重连、销毁和离线状态测试。
- [ ] 增加端到端验收流程：登录 -> 上传文件 -> 创建任务 -> 派发 -> 安全确认 -> 启动 -> 暂停/恢复 -> 完成或失败。
- [ ] 联调完成后执行 `npm run lint` 和 `npm run build`，并人工检查 lint 自动修复结果。

### P2.3 生产环境

- [ ] 生产构建使用独立环境变量，不提交 `.env`、token、密码或真实设备凭据。
- [ ] 确认 CORS、预签名 URL 和 WebSocket 鉴权策略与前端部署地址一致。
- [ ] 处理 API、WebSocket、对象存储和设备网络异常的统一用户提示。
- [ ] 确认生产环境不会启用匿名 WebSocket，也不会误启用 mock 数据。

## 推荐实施顺序

```text
环境变量与请求层
        ↓
API 模块和数据格式统一
        ↓
登录/权限
        ↓
打印机看板与控制
        ↓
文件库
        ↓
任务队列与安全打印流程
        ↓
WebSocket 快照/离线/任务消息
        ↓
错误场景、测试和生产加固
```

## 第一版前端完成定义

- [ ] ADMIN 和 OPERATOR 能登录，并按角色看到正确菜单和操作按钮。
- [ ] 能查看打印机分页和看板状态，ADMIN 能维护打印机及网格位置。
- [ ] 能上传、筛选、下载、删除文件并创建文件夹。
- [ ] 能创建任务、派发任务、安全确认、安全启动、暂停、取消和急停。
- [ ] REST 初始数据与 WebSocket 设备/任务状态能够正确同步。
- [ ] 401、403、资源不存在、设备离线、设备忙碌和网络错误都有明确提示。
- [ ] mock 模式与真实接口模式可切换，且不影响业务组件调用方式。
- [ ] `npm run lint`、`npm run build` 通过，且没有提交环境凭据或后端内部敏感字段。
